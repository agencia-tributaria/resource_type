name: 'resource_type'
description: 'Expandir WADL y crear los paquetes Maven correspondientes'
inputs:
  changes:
    description: 'Cambios realizados en el commit que provocó la ejecución de esta acción'
    required: true
outputs:
  time:
    description: 'Momento en el que se ejecuta la acción'
runs:
  using: 'node16'
  main: 'index.js'
